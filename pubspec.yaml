name: newsapp
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  get: ^4.6.6
  dio: ^5.4.3+1
  url_launcher: ^6.3.0
  flutter:
    sdk: flutter
  image_picker: ^1.1.2
  camera: ^0.11.0+1
  image: ^3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
